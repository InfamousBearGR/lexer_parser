PROGRAM my_program
    /*
        this program is
        only a test
    */

    /*
        no rights reserved
    */
    FUNCTION mean(INTEGER x, INTEGER y)
        RETURN (x+y)/2;
    END_FUNCTION

    STARTMAIN
        VARS 
        INTEGER a, b, c;
        
        % calculate the mean of two numbers
        a = mean(b, c);
    ENDMAIN

